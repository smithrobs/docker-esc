name: docker-ecs
runtime:
  name: nodejs
  options:
    packagemanager: npm
description: A Pulumi program that demonstrates using ESC with a docker container
config:
  pulumi:tags:
    value:
      pulumi:template: typescript
